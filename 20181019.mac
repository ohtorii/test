/*
dllfuncstr関数が ; で終端していないときの不具合について。

fooラベル中のif文のコメント有無で以下のように動作が変わります。

・if文のコメントを外してif文を有効にする。
　→「式がおかしいです」とエラー表示される。（正常動作）

・if文のコメントをそのままにする。
　→「式がおかしいです」とエラー表示されない。（誤動作）
*/
foo:
	$$parent = dllfuncstr(0, "", "")
	/*if(false){
	}*/
	return $$parent